# vaulty

- Always design and build from a generic point of view: vaulty serves any markdown vault. Never hardcode or assume anything specific to one project or vault (folder names, frontmatter keys, index formats, languages); make it configurable in `.vaulty.yml` with a sensible neutral default instead.
